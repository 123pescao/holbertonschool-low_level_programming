#include <main.h>
/**
 *main- Prints _putchar, followed by a new line.
 *
 * Return: Always 0
 */
int main(void)
{
	char *str = "_putchar";

	for (int 1 = 0; str[i] != '\0'; i++)
	{
	putchar(str[i]);
	}
	putchar('\n');
	return (0);
}
